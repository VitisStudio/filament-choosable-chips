# Approved copy

Use only these strings in the imagery. Nothing longer.

## Product name (required)
- Primary: **Choosable Chips**
- Kicker (optional, smaller, above or below the name): **for Filament**

## Tagline (hero only, pick at most one)
- "Options as colorful, dismissable chips."
- "Turn checkbox & radio options into chips."
- "One field. Any option. As a chip."

## Chip labels to render (real values)
Blue · Red · Green · Amber · Indigo · Purple · Teal · Cyan · Pro · Enterprise

## Optional code motif (hero only — stylized, not literal)
```php
ChoosableChips::make('tags')
    ->multiple()
    ->options([...])
    ->colors([...])
    ->icons([...]);
```

## Do not include
- The full package name `vitisstudio/filament-choosable-chips`
- Version numbers
- Any paragraph of prose
